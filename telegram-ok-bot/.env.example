TELEGRAM_BOT_TOKEN=
# Полный URL воркера helloword (…workers.dev), без слэша в конце. Не подставляйте URL Cloudflare Pages — иначе POST даст HTML 404.
# Можно совпадать с VITE_HELLOWORD_BASE_URL в корне. Локально: http://127.0.0.1:8787 (wrangler dev helloword).
HELLOWORD_BASE_URL=
# Тот же секрет, что: wrangler secret put TELEGRAM_BOT_INGEST_SECRET (из каталога helloword)
TELEGRAM_BOT_INGEST_SECRET=
