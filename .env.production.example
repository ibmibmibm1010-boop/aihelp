# Сборка продакшена (Vite подставляет только переменные с префиксом VITE_).
# Скопируйте в .env.production ЛИБО задайте секрет VITE_HELLOWORD_BASE_URL в GitHub Actions.
# Без завершающего /

VITE_HELLOWORD_BASE_URL=https://helloword.ibmibmibm1010.workers.dev
