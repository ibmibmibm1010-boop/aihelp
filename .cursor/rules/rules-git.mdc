---
alwaysApply: true
---

**Репозиторий проекта (единственный по умолчанию):** `https://github.com/ibmibmibm1010-boop/aihelp` — remote **`origin`**.

- Делай **`git push`**, `git pull`, обсуждение веток и PR **только** с/на **aihelp** (`origin/main`), пока пользователь **явно** не попросит иной URL или второй remote.
- **Не** дублируй пуши во второй репозиторий (в т.ч. старое зеркало `ibmibmibm1010-boop/-2-`, `dash2` и аналоги) и **не** добавляй такие remotes без прямой просьбы.
- Не предлагай «запушить и туда же в -2-» в качестве стандартного шага.
